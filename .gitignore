node_modules/
.next/
*.Zone.Identifier

# env files (nunca commitear secrets)
.env
.env.local
.env*.local

# estado de sesión de Claude (runtime, no versionar)
.claude/state/

# lockfile generado por tooling de skills (no es del CRM)
skills-lock.json

# generado por docs/dow-summit-2026/build-artifact.py
docs/dow-summit-2026/revision-artifact.html

node_modules/
.next/
*.Zone.Identifier

# env files (nunca commitear secrets)
.env
.env.local
.env*.local

# estado de sesión de Claude (runtime, no versionar)
.claude/state/

# lockfile generado por tooling de skills (no es del CRM)
skills-lock.json

# planillas y documentos de negocio sueltos en la raíz: son insumo, no código.
# Los datos reales de cliente viven fuera del repo, en
# /home/jzenteno/projects/_archivo-crm-containers/. Las fuentes que SÍ se versionan
# (reglas de free time de M5) están en docs/m5/fuentes/ y no las alcanza este patrón.
/*.xlsx
/*.docx
/*.pdf
/*.html

# Material de trabajo de John que queda LOCAL (pedido explícito 2026-08-01)
docs/Modelo LOGIN VGM SISTEMA E-Cargo.xlsx

# App Android: la llave de firma y su password NUNCA se versionan (John guarda copia
# en Drive); los binarios de build tampoco (se distribuyen por GitHub Release).
android/crm-ssb.keystore
android/keystore-password.txt
android/*.apk
android/*.aab
android/app/
android/build/
android/gradle*
android/.gradle/
android/*.log
android/manifest-checksum.txt
